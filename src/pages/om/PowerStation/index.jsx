import React, {
  Component,
  PureComponent,
  lazy,
  useEffect,
  useMemo,
  useRef,
  useState,
} from 'react';
import './style.less';

import { Form, Input, Button, Checkbox } from 'antd';

const PowerStation = () => {
  return <div className={'PowerStation'}>PowerStation</div>;
};

export default PowerStation;
